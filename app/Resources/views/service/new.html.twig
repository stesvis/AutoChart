{% extends 'base.html.twig' %}

{% block body %}
    <h2>New Service</h2>

    {{ form(serviceForm) }}
{% endblock %}

{% block rightSidebar %}
    <a
            href="{{ path('service_list') }}"
            class="btn btn-info btn-block"
            role="button">View All</a>

{% endblock %}
