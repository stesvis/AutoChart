{% extends 'base.html.twig' %}

{% block body %}
    <h2>New Task Field</h2>

    {{ form(taskfieldForm) }}

{% endblock %}

{% block rightSidebar %}
    <a
            href="{{ path('taskfield_list') }}"
            class="btn btn-info btn-block"
            role="button">View All</a>

{% endblock %}
